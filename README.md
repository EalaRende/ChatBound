# ChatBound

ChatBound is a Dalamud plugin for filtering selected incoming FFXIV chat through configurable local profiles and dictionaries.

Use `/chatbound` to open the configuration window. Choose the chat channels to process, select a profile, and decide how words outside the active dictionary are displayed.

ChatBound only changes what is shown on the local client. It does not modify outgoing chat, server messages, or another player's client.